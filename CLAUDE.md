# CLAUDE.md

This project's agent-facing index lives in `AGENTS.md` at the repo root, kept there (not here) so
non-Claude harnesses (Codex, Cursor, etc.) pick it up by the same cross-tool convention. Import it below
rather than duplicating it — if you're editing the index, edit `AGENTS.md`, not this file.

@AGENTS.md
